# Travel Planner MVP

The reviewable app lives in `web/`.

```bash
cd web
npm run dev
```

Primary review path: `/` -> `/discovery/[sessionId]` -> `/preferences/[sessionId]` -> `/trips/[sessionId]`.

See `web/docs/mvp-launch-checklist.md` for setup, verification, fallbacks, and MVP limits.
